import React from 'react';

const Subtitle = ({ text }) => {
    return(
        <>
            <div className="subtitle">
                <h3>{text}</h3>
            </div>  
        </>
    )
};

export default Subtitle;